extends ../layouts/layout-registration

block append content

    .registration-form
        .registration-form-headline
            РЕГИСТРАЦИЯ ПОЛИГРАФИИ
        form(id='registration-form', method='post', action='/registration-link')
            fieldset
                if errors
                    ul
                        each error, key in errors
                            li #{error}
                input(type='hidden', name='#{_csrf.parameterName}', value='#{_csrf.token}')
                input#hash(type='hidden', name='hash', value='#{hash}')
                .text-fields
                    .field-email
                        |E-mail для регистрации
                        .input-wrapper
                            input.form-control#email-input(name='email', type='text')
                            .invalid-email
                    .field-password
                        |Пароль
                        .input-wrapper
                            input.form-control#password-input(name='password', type='password')
                            .invalid-password
                    .field-name
                        | Название полиграфии
                        .input-wrapper
                            input.form-control#name-input(name='name', type='text')
                            .invalid-name
                    .field-address
                        | Адрес
                        .input-wrapper
                            input.form-control#address-input(name='address', type='text')
                            .invalid-address
                    .field-phone
                        | Телефон
                        .input-wrapper
                            input.form-control#phone-input(name='phone', type='text')
                            .invalid-phone
                    .field-public-email
                        | Публичный e-mail
                        .input-wrapper
                            input.form-control#email-public-input(name='emailPublic', type='text')
                            .invalid-public-email
                    .field-site
                        | Сайт
                        input.form-control#website-input(name='website', type='text')
                    .field-text-area
                        | Описание
                        textarea.form-control#text-area-field(name='textArea')
            fieldset
                .field-checkboxes
                    .field-order-by-email.field-checkbox
                        input.js-obe.checkbox(id='orderByEmail', name='orderByEmail', type='checkbox')
                        label(for='orderByEmail') Принимаем заказы по e-mail
                    .field-issue-check.field-checkbox
                        input.js-wtc.checkbox(id='writesTheCheck', name='writesTheCheck', type='checkbox')
                        label(for='writesTheCheck') Выдаем чек
                    .field-payment
                        | Принимаем оплату:
                        .input-wrapper
                            for paymentMethod in paymentMethods
                                .field-checkbox
                                    input.js-pm.checkbox(id='payment#{paymentMethod.id}', name='paymentMethods', value='#{paymentMethod.id}', type='checkbox')
                                    label(for='payment'+paymentMethod.id)=paymentMethod.name
                            .invalid-payment-method
                    .field-delivery
                        | Доставка готового заказа:
                        .input-wrapper
                            for deliveryMethod in deliveryMethods
                                .field-checkbox
                                    input.js-dm.checkbox(id='delivery#{deliveryMethod.id}', name='deliveryMethods', value='#{deliveryMethod.id}', type='checkbox')
                                    label(for='delivery'+deliveryMethod.id)=deliveryMethod.name
                            .invalid-delivery-method
                    .field-services_id
                        | Предоставляемые услуги:
                        .input-wrapper
                            for service in services_id
                                .field-checkbox
                                    input.js-s.checkbox(id='service#{service.id}', name='services_id', value='#{service.id}', type='checkbox')
                                    label(for='service'+service.id)=service.name
                            .invalid-services_id
                button.submit-registration-form(type="submit", id="submit-registration-form") Сохранить данные
